# StratumGenesis 前后端打通原型 · 使用说明

## 1. 启动本地服务

本版本前端不再使用硬编码 mock 数据，需要先启动本地 Python 后端：

```bash
python server.py
```

服务监听：

```
http://127.0.0.1:28417
```

- 端口固定为 **28417**（不使用 8000/8080/3000/5000 等常见端口）；
- 仅允许 localhost 本机访问，不支持公网；
- 使用 Python 标准库 `http.server`，无第三方 Web 框架；
- 全部状态保存在内存，服务关闭后所有数据清空。

启动成功后控制台会显示当前主链高度（预沉积 102 个演示区块），然后打开浏览器访问：

```
http://127.0.0.1:28417/index.html
```

> 不要直接用 `file://` 打开 index.html：页面需要调用后端 API，必须由 server.py 提供 HTTP 访问。

## 2. 页面交互点

| 交互 | 操作 | 后端行为 |
|---|---|---|
| 加载地层 | 打开页面 | `GET /chain-state` 拉取主链、纪元快照、休眠分支；预沉积 102 区块渲染完整剖面，第 0 纪元播放抬升下沉动画 |
| 提交提案 | 输入特性描述，点击【推演】 | `POST /propose`：解析文本 → 生成演示代码与测试用例 → ECDSA 签名 → 6 阶段校验 → 冲突加权投票 → 成功上链或进入休眠分支 |
| 校验通过 | 普通提案文本 | 返回 `success=true`；页面播放粒子沉积 + 新岩层凝聚动画，自动打开沙箱展示服务端生成的演示代码 |
| 校验被拒 | 文本含「修改内核 / 严格求值 / 可变绑定」等 | 返回 `success=false`；页面播放岩层碎裂消散动画并显示原因（区块存入休眠分支，不修改账本） |
| 查看岩层 | 点击任意岩层 | 右侧详情面板显示高度、纪元、矿工署名、描述、演示代码、测试用例 |
| 沙箱运行 | 详情面板【打开沙箱运行】或弹窗内点击【运行】 | `POST /eval-novscript` 调用真实 NovScript 解释器，返回结果或中文错误类型 |
| 钻探考古 | 点击「第 0 纪元」标签上的【钻探考古】 | 进入考古视图，手电筒径向光照跟随鼠标照亮半透明古岩层；【退出钻探】返回 |
| 查看纪元摘要（v0.3 阶段 B） | 点击已归档纪元标签下的摘要卡标题 | 原生可折叠 `<details>` 卡片展示该纪元最终摘要、胜者、三份候选与权重、平票标记；数据来自 `GET /chain-state` 追加的 `epochs[i].summary` |
| 程序画廊 | 点击顶部【程序画廊】 | 左侧滑出画廊，作品代码由真实后端沙箱执行 |

## 3. 提案文本与演示代码

服务端把提案文本映射为创世内核语法范围内的演示代码（真实内核仅支持整数、函数、bind 与 `+` 加法）：

- 「函数 / lambda / 增量」→ 增量函数演示
- 「绑定 / bind / 变量」→ 绑定聚合演示
- 「+ / 求和 / 加法」→ 加法演示
- 「注释」→ 行注释演示
- 其他 → 内核算术演示

映射为简化 mock 行为，详细见 `API_SPEC.md`。

## 4. 运行测试

```bash
# 先停止手动运行的 server.py（测试会占用 28417 端口）
python -m unittest discover -s tests -v
python -m compileall -q .
```

`tests/test_http_api.py` 会自行启动一个服务实例，覆盖：

- chain-state 结构（创世块、矿工、纪元快照、休眠分支）
- 合法提案上链并增加高度
- 内核冲突提案进入休眠分支且高度不变
- 未知矿工身份被拒绝
- 沙箱执行：成功 / 语法错误 / 未绑定名称
- index.html 可访问

## 5. 纪元摘要（v0.3 阶段 B）

每个纪元在主链越过边界（高度 100/200/300…）时被归档，并**确定该纪元的官方摘要**：

- 生成：3 套确定性规则模板（特性关键词词频 Top / 贡献者分布 / 首末特性串讲），全程 mock，不调用真实 LLM；
- 投票：按历史贡献权重（仅主链、截止纪元边界）加权，平票按模板名排序取首并记录；
- 不可变：确定后摘要不再变化，重启加载存档后逐字段一致（摘要不落盘，由主链重放确定性重建）；
- 前端：已归档纪元标签下出现可折叠摘要卡，点击展开查看胜者、最终文本与候选权重；
- 未来替换：接入真实 LLM 时替换 `epoch_summary.py` 的生成函数即可，数据模型与投票语义不变（详见 `EPOCH_SUMMARY_IMPLEMENTATION.md`）。

## 6. 存档、导出与复位（v0.3 新增）

### 存档路径

- 默认存档：`data/chain_v1.json`（项目根目录下，已在 .gitignore 中排除）；
- 有存档时启动会**加载继续**（不再重新预沉积）；无存档时维持创世 + 预沉积 102 演示块并立即落盘；
- 每次链状态变更（成功上链 / 候选入休眠分支）后自动保存；
- 存档损坏或格式版本不匹配时，启动会明确报错并拒绝运行，提示使用 `--fresh`。

### 常用命令

```bash
python server.py                    # 加载存档继续 / 无存档则预沉积 102 块
python server.py --fresh            # 忽略存档，回到预沉积 102 块初始态
python server.py --export data/copy.json   # 校验并导出存档副本后退出
python persistence.py export data/copy.json # 等价 CLI
```

> 注意：矿工私钥为原型本地演示密钥，明文保存在存档中，无生产安全承诺，仅限本机实验使用。

## 7. 已知限制（与全局项目一致）

1. 无 P2P、无磁盘持久化、无公网部署、无身份鉴权、无 HTTPS、无数据库。
2. ECDSA 仅为实验原型签名，不是生产级密码学。
3. 沙箱是教学级纯计算隔离，不是生产安全容器。
4. 未来 LLM 摘要存在信息衰减与幻觉风险；摘要链仅线性低速增长。
5. 共识机制仅适配小规模仿真网络。
6. 纯非金融激励存在参与者流失风险。
7. 无任何可交易代币或现实金融功能；UTXO 余额与投票权重完全隔离。
