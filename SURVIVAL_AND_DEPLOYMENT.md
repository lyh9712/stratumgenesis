# StratumGenesis · 存活与部署策略 + 许可方案（监督者整理）

> 目的：让项目**在没有作者运营服务器的情况下**也能上互联网、被人看到、被人接手继续活下去。
> 状态：v0.3 阶段 D 之后（120 项测试通过）。本文为策略与分工底稿，**不含实现代码**。
> 作者：监督者（本会话 agent）整理；未经人工确认前不作为对外承诺。

---

## 1. 核心策略：把「项目活下去」与「作者运维服务器」解耦

| 层 | 谁来做 | 作者义务 |
|---|---|---|
| **代码仓库**（GitHub 公开） | 作者 | 一次性 |
| **只读展馆**（GitHub Pages 静态） | 作者 | 零服务器、零月费、零维护（只推文件） |
| **活的 hub**（能提交提案的共享链） | **任何有实力的人** | **作者零义务**：提供 Dockerfile + 一键部署按钮 + 部署指南即可 |

关键事实：本项目**单机仿真、无 P2P、无真实资产、工作负载极轻**（mock 分词计数），
因此「挖矿」完全可以在**访问者的浏览器里**执行——这是「零服务器也能全功能」的技术前提。

---

## 2. 上线路线（按作者负担从低到高）

### 2.1 零服务器四件套（作者一个人可完成）
1. **仓库门面**：根目录 `README.md`（一句话定位 + 30 秒 GIF + 截图 + 许可声明）。
2. **Pages 只读展馆**：用 `persistence.export_chain()` 导出 `chain_state.json` 放进仓库，
   前端在拿不到后端时读它 → 任何人点链接即可「钻探考古」，看地层剖面。
3. **Releases 一键包**：`start.bat`（查 Python → 缺 `ecdsa` 自动装 → 起服务 → 自动开浏览器）。
4. **Codespaces 徽章**：README 放「Open in Codespaces」按钮；各人自带容器，作者不承担运行成本。

> ⚠️ **红线：静态导出必须剥离 `miners` 字段**。`persistence.py` 的存档**含明文私钥**
> （`data/chain_v1.json` 实测含 3 组矿工私钥）。公开链数据没问题（白皮书 §14.5 本就要公开数据集），
> **连带私钥一起发布等于泄露**。导出脚本必须显式删除 `miners`（或仅保留 `public_key_b64`/`label`）。

### 2.2 浏览器原生版（让 Pages 变成「全功能、可玩」）
| 方式 | 做法 | 代价 |
|---|---|---|
| **A. Pyodide（推荐先试）** | 浏览器加载 CPython(WASM)，**原样运行现有 Python 模块**（`block_model`/`novscript`/`block_validator`/`chain_store`/`persistence`），`micropip` 装纯 Python 的 `ecdsa`，链存 IndexedDB 或导出 JSON | 几乎零改写；首次加载十几 MB WASM、启动数秒。**可在浏览器内跑通 120 项测试**，是极强可信度展示 |
| **B. 手写 JS/WASM 版** | 解释器/校验器/链全部重写为 JS | 最丝滑，但必须**逐字节复现 `canonical_bytes()`**（`sort_keys` / `ensure_ascii=False` / 紧凑分隔符 / 十六进制字段），且 WebCrypto 输出 raw `r‖s` 而 Python 侧 `sign_deterministic` 为 **DER**，需做转换 |
| **C. 纯静态只读** | 只展示预导出数据 | 最省事，但**不能提案**，只解决「看到」 |

选 A 的效果：Pages 上就是完整可玩链，链存在访问者自己浏览器里，**作者零成本、零运维**；
每人一片自己的地层，可导出链文件与朋友继续长——本身即传播点。

### 2.3 「谁有实力谁去部署」机制化
1. **`Dockerfile`**（一行 `docker run` 起 hub）——当前缺失，是部署最大摩擦；
2. **`DEPLOY.md`**（Render / Fly / PythonAnywhere 免费层逐步指南 + 部署者义务清单）；
3. **`render.yaml` 蓝图**（一键部署按钮）→ 任何人点一下就能替全世界开一个 hub；
4. **hub 注册表**（README 表格：谁跑了 hub → 贴链接）→ 多 hub 并存、数据可互导，抗单点；
5. **数据自动归档**：hub 运营者用 GitHub Actions 定时导出链数据提交回仓库
   （Actions 当免费定时任务，无需服务器）→ 某个 hub 关站，历史仍留存在仓库。

---

## 3. 部署者须知（必须写进 `DEPLOY.md`）

1. **服务端持私钥 = 公网可冒充**：现有 `MinerRegistry` 存全部矿工私钥、`/propose` 替用户签名；
   公网开放后任何人可冒充任何身份。正经做法：签名移到客户端（WebCrypto / Pyodide 内生成密钥），服务端只验签。
2. **无金融激励 + 公网匿名 = 垃圾提案**：需轻量防滥用（提案限频、PoI 门槛、邀请制纪元、只读访客模式）。
3. **免费层会冷启动/休眠**，不适合长期常驻 → 「多 hub + 数据归档」比「一个稳定 hub」更现实。
4. **禁止绑定作者个人资源**：不依赖作者的 API key、付费服务或某个模型厂商；
   真实 LLM 仅作可选模式，且使用参与者自己的 key 或本地模型。

---

## 4. 许可方案（作者要求：**不得被用于盈利** + **保留版权与署名**）

### 4.1 推荐组合
| 对象 | 许可 | 说明 |
|---|---|---|
| **代码**（`*.py`、`index.html` 等） | **PolyForm Noncommercial License 1.0.0** | 软件专用、明文禁止商业使用；允许非商业使用/修改/再分发，须保留许可与署名 |
| **文档**（白皮书、实现说明、本文档等） | **CC BY-NC-SA 4.0** | 署名 + 非商业 + 相同方式共享（CC 不建议用于代码，但适合文档） |
| **项目名称与标识** | 单独的名称/商标使用声明 | 许可是著作权授权，**不自动授予商标权**；名称不得被用于暗示官方背书或商业产品 |
| **中文补充说明** | `LICENSE-SCOPE.md` | NC 条款模糊，需用平实中文界定「什么算商业使用」 |

### 4.2 `LICENSE-SCOPE.md` 应明确的边界（建议条文要点）
- **允许**：个人学习与娱乐；教学与课堂演示；学术研究与论文引用（须署名）；**免费公开**的 hub 部署
  （无收费、无订阅、无广告变现）；非营利组织的内部实验；导出与分享链数据（须剥离私钥、保留署名）。
- **禁止**：任何收费服务或付费墙；订阅制/打赏墙/广告变现；打包进商业产品或商业 SaaS；
  以本项目的代码或数据作为商业交付物的组成部分；移除或篡改署名与免责声明。
- **部署者义务**：保留 `LICENSE`、`NOTICE` 与免责声明；在显著位置标明「非官方部署」；
  不得使用项目名称暗示官方背书。
- **免责与性质重申**（须随许可一并保留）：单机仿真实验原型、非生产系统、**无任何可交易代币**、
  链内凭证无现实价值、沙箱为教学级隔离、LLM 摘要存在信息衰减与幻觉风险。

### 4.3 备选方案与取舍（作者需知情）
| 方案 | 能阻止免费搭车/盈利吗 | 代价 |
|---|---|---|
| **PolyForm NC / CC BY-NC-SA**（推荐） | ✅ 明文禁止商业使用 | **不是 OSI 开源许可**：不能被称作 "open source"；企业开发者、Linux 发行版打包、部分学术合作会因此回避 |
| **AGPL-3.0 + 商业授权（双许可）** | ⚠️ 允许商业使用，但网络服务必须开源；闭源商用需向你付费 | 保住 "open source" 名分，是「防 SaaS 白嫖又能收费」的标准做法 |
| **BUSL-1.1（先闭后开）** | ⚠️ 禁止「竞争性」商业使用，允许其他商用；到期自动转 Apache-2.0 | 需要设定转换年限；对"任何盈利都禁止"不满足 |
| **MIT / Apache-2.0** | ❌ 完全允许他人商业化 | 与你的要求冲突，不推荐 |

### 4.4 三条诚实提醒（重要）
1. **著作权只保护表达，不保护想法**：许可能阻止别人**复制你的代码**，但**无法阻止别人照你的思路重写一个**。
   因此真正的护城河是：**署名与先发记录**（公开仓库时间戳、白皮书日期）+ 持续演进的实现 + 社区。
2. **AI 生成内容的著作权存在不确定性**：本项目代码由 AI 智能体在你的指令下产出；多数司法辖区
   对「纯 AI 生成」的可版权性持保留态度。实践上你作为项目组织者主张权利，但**这不是法律意见**，
   若涉及商业维权，请咨询专业人士。
3. **NC 会挡住一部分参与者**，这与「让更多人参与」存在张力。折中：核心代码用 NC，
   但**明确允许**非营利/教学/研究的免费部署与再分发，并把「申请商业授权」的联络方式写进 README。

---

## 5. 建议排期（冻结结束后按此分工）

**第一批（作者一人可完成，零服务器）**
1. `LICENSE`（PolyForm NC）+ `LICENSE-SCOPE.md`（中文边界）+ `NOTICE` + README 许可章节；
2. `README.md`（新文件：定位句 + GIF + 截图 + 三种上手路径 + 五个徽章）；
3. `start.bat` 一键启动；
4. `Dockerfile` + `DEPLOY.md` + `render.yaml` + 私钥剥离的静态导出脚本 + Pages 静态降级。

**第二批（最强模型：让项目「活得久」的关键一击）**
5. **Pyodide 浏览器原生版**：Pages 上跑全功能链；顺带把 120 项测试搬进浏览器。

**第三批（研究性）**
6. 客户端签名 + 只转发 hub（「本机挖矿」路线）；同一高度候选收集窗口规则设计说明。

---

## 6. 边界与免责（随本文档一并保留）

- 本文档为策略建议，不构成法律意见；
- 项目性质不变：单机仿真实验原型、非生产系统、**无任何可交易代币或现实金融价值**、
  无 P2P、无公网鉴权、沙箱为教学级隔离；
- 任何部署者应自行评估合规与安全风险，并保留上述声明。
