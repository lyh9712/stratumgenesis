# StratumGenesis UI 样式变更说明（UI_CHANGE_NOTE.md）

> 日期：2026-09-09
> 范围：仅修改 `index.html` 的 `<style>` 样式区块
> 版本：v0.2 前后端打通版（地质山体剖面视觉强化 · 第二轮）

## 变更声明（重要）

本次更新**只修改 CSS**：

- ✅ **未改动任何 JavaScript 代码**（`<script>` 区块与修改前逐字节一致，已通过备份对比验证）
- ✅ **未改动任何 HTML DOM 节点结构**（标签结构校验通过，无增删元素、无改类名/ID）
- ✅ **未改动任何 API 请求**（仍为 `/chain-state`、`/propose`、`/eval-novscript`）
- ✅ **可直接继续对接现有 `server.py`**（127.0.0.1:28417），无需重启服务、无需改动后端
- ✅ 全部原有交互保留：点击岩层详情、钻探考古手电筒、推演-粒子-凝聚/碎裂动画、程序画廊、纪元抬升

## 本次视觉改动点

### 1. 岩层物理厚度（上下内边距）

- `.layer` 高度提升至 `50px`，新增**上下内边距 `padding: 7px 14px`**，配合 `display:flex` 内容居中，制造岩层实体厚度感；
- 新岩层凝聚动画 `@keyframes condense` 终点高度同步为 `50px`，推演成功后新层与既有层厚度完全一致、无跳动。

### 2. 岩石断面层理纹理（纯 CSS 渐变，无外部图片）

- 活跃地层保持**琥珀色调**：每层为「顶部高光/底部阴影渐变 + `repeating-linear-gradient` 水平层理条带 + 琥珀主色」三层叠加；
- **归档古岩层改为暗褐色色调**：`.layer.archived` 直接覆盖为深棕渐变（`#5a422b → #3e2c1a → #241708`）+ 层理条带 + 底部沉阴影，不再是「琥珀变暗」，视觉上明确区分活跃层与地下古岩层；
- 上下边缘明暗渐变（`::after`）保留并强化：顶部亮、底部暗，模拟岩石切割断面；
- 岩层颗粒噪点（`::before` 内联 SVG feTurbulence）保留，无任何外部图片资源。

### 3. 纪元抬升：整体向下沉入地下的位移动画

- 归档纪元地层除透明度变化外，增加 `transform: translateY(30px)` **向下滑动位移动画**（`transform 1.6s cubic-bezier` 过渡），模拟整块地层向下沉入地下；
- 归档纪元分隔标签同步 `translateY(10px)` 下沉；
- `z-index` 层级保护保留：活跃地层（`z-index:1`）始终绘制在滑落的古地层（`z-index:0`）之上，避免遮挡与误点。

### 4. 岩层内部文字可读性

- 特性名称 `.layer .nm`：`12.5px → 13px`，颜色加深至实色；
- 高度编号 `.layer .h`：保持 `11px`，颜色略加深；
- 矿工署名 `.layer .sig`：`11px`，颜色加深（透明度 `.72 → .8`）。

### 5. 创世内核花岗岩层强化

- 高度 `150px → 172px`，在普通沉积岩层中明显更厚重；
- 灰调渐变加重：`#b8afa1 → #8f8678 → #6a6256`，比普通岩层深且更「重」；
- 花岗岩颗粒斑点扩至 7 处径向渐变 + 新增 90° 方向竖向纹理条带；
- 阴影强化：顶部 `3px` 高光 + 底部 `10px/22px` 深阴影 + 整体 `60px` 环境暗部，断面立体感与「永恒基岩」的厚重感更突出；
- 文字与徽章颜色随深色花岗岩微调，保持可读性。

## 保留不变的交互（复验清单）

- 点击岩层 → 右侧详情面板弹出（高度/纪元/矿工/描述/代码/测试用例）
- 钻探考古 → 手电筒径向光照跟随鼠标照亮古岩层，【退出钻探】返回
- 推演 → 粒子沉积 → 岩层凝聚（高度 50px）/ 校验失败 → 碎裂消散
- 纪元抬升动画（页载 900ms 后整体下沉）
- 程序画廊侧边面板 → 沙箱弹窗 → `/eval-novscript` 真实执行

## 验证方式

```bash
python server.py                       # 服务已在运行则无需重启
# 浏览器打开
# http://127.0.0.1:28417/index.html
# 或直接刷新页面即可看到新样式（服务按请求读取磁盘上的 index.html）
```

观察点：

1. 每个岩层带颗粒纹理、层理条带、上下明暗断面，厚度明显；
2. 页面加载约 0.9s 后，第 0 纪元整块地层变为**暗褐色**并整体**向下滑动 30px** 沉入地下；
3. 创世内核层明显更厚重，灰调花岗岩质感突出；
4. 推演合法提案：新层凝聚高度 50px 与既有层一致；
5. 提交「修改创世内核…」：碎裂动画与休眠分支行为不变；
6. 详情面板、钻探手电筒、程序画廊全部照常。

## 已知说明

- 本次仍为纯视觉变更，服务端业务逻辑、API 契约、区块/账本/投票行为完全未变；
- 拒绝提案时的临时碎裂层由 JS 内联样式控制高度（32px），按约束未触碰 JS，该层约 0.9 秒即消散，不影响整体厚度观感；
- 全部纹理使用 CSS 渐变与内联 SVG data URI，无外部图片、无需联网。
