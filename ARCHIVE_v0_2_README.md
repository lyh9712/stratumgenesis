# StratumGenesis 地层创世实验 · v0.2 归档总说明

> **归档入口文档**：本文件是整个 v0.2 归档包的第一入口，其他文档见文末「归档包内容索引」。

---

## 1. 项目简介

| 项 | 内容 |
|---|---|
| 项目名称 | StratumGenesis（地层创世实验，前身 TokenMint） |
| 归档版本 | **v0.2（前后端打通 · 单机仿真归档版）** |
| 归档日期 | 2026-09-09 |
| 定位 | 学术概念艺术 + 人机协同编程语言演化**单机仿真原型** |
| 核心隐喻 | 地质地层；每 100 区块 = 1 纪元；创世内核 NovScript 永久不可修改 |
| 性质声明 | 非商用产品、非公链、非加密货币，**无任何可交易金融代币** |

StratumGenesis 是一个小规模单机仿真实验：参与者通过提交语言扩展提案，共同从零创造一门前所未有的编程语言 NovScript。每一次成功提案被永久写入一条「地层」式区块链结构中——链的主体不是转账记录，而是一门语言从创世内核开始一点点演化、分叉、沉淀的完整历史。

## 2. 核心实现目标与定位

- **创世内核 NovScript**：S 表达式、惰性求值、`bind` 不可变绑定、仅整数与函数为一等值、仅内置 `+` 加法原语；内核永久不可修改。
- **PoI（推理工作量证明）**：矿工提交语言扩展提案，使用 mock 标准分词器统计词元数作为工作量；不调用真实 LLM。
- **区块与校验**：每个区块承载一份语言扩展提案，经 6 阶段合法性校验流水线（ECDSA 签名 → 结构/父区块 → 创世内核兼容 → mock PoI → demo/测试解析 → 沙箱运行 → UTXO 校验）。
- **共识仿真**：候选池按（父区块、高度）分组，历史贡献（主链累计标准词元数）加权投票选出胜者；落选/平票候选进入休眠分支。
- **UTXO 账本**：链内凭证仅实验内部记账，余额与投票权重**完全隔离**，无现实货币价值。
- **纪元基础框架**：`epoch = height // 100` 自动编号、纪元边界事件检测、内存快照与归档只读标记（LLM 摘要等留待后续）。
- **前后端打通**：标准库 `http.server` 提供 3 个 JSON API，前端全部数据来自真实后端业务逻辑。

**一句话定位**：v0.2 是一套「跑得起来、看得见、能演示」的单机仿真原型，用于概念与实验演示，不是生产系统。

## 3. 快速启动指南

```bash
# 1) 进入项目目录
cd <项目目录>

# 2) 启动本地服务（监听 127.0.0.1:28417，仅本机可访问）
python server.py
```

```text
启动成功后会输出：
  StratumGenesis 本地服务已启动（内存仿真，关闭即清空）
  访问地址：http://127.0.0.1:28417/index.html
  当前主链高度：102
```

```bash
# 3) 浏览器打开前端页面
#    手动打开：http://127.0.0.1:28417/index.html
```

> 注意：前端必须由 server.py 提供 HTTP 访问，不要用 `file://` 直接打开 index.html（页面依赖后端 API）。
> 端口固定为 28417，若提示端口被占用，请先结束旧的 `python server.py` 进程。

## 4. 系统整体架构总览（模块结构图 · 文本版）

```text
┌────────────────────────────────────────────────────────────────┐
│  index.html  前端地层可视化                                      │
│  （地层剖面 / 推演动画 / 岩层详情 / 钻探考古 / 程序画廊 / 沙箱弹窗）│
└──────────────┬─────────────────────────────────────────────────┘
               │ 同源 fetch：GET /chain-state · POST /propose · POST /eval-novscript
               ▼
┌────────────────────────────────────────────────────────────────┐
│  server.py  HTTP 服务（http.server，127.0.0.1:28417，内存态）    │
│  ├─ 静态文件：/index.html                                        │
│  ├─ GET  /chain-state      主链/纪元快照/休眠分支/矿工            │
│  ├─ POST /propose          提案→生成代码→签名→候选池→投票→上链    │
│  └─ POST /eval-novscript   真实 NovScript 沙箱执行               │
└──────────────┬─────────────────────────────────────────────────┘
               │
               ▼ 业务模块（单机内存）
┌────────────────────────────────────────────────────────────────┐
│ novscript/        NovScript 沙箱解释器                          │
│   lexer→parser→AST→evaluator→sandbox（惰性求值、thunk、循环检测）│
├────────────────────────────────────────────────────────────────┤
│ block_model.py    区块数据模型 + SHA256 + canonical_bytes        │
│ mock_tokenizer.py PoI mock 标准词元计数                          │
│ block_validator.py 6 阶段校验流水线                              │
│ candidate_pool.py 候选区块池（按父哈希+高度分组）                 │
│ weight_calculator.py 历史贡献权重（仅主链区块计入）               │
│ conflict_voter.py 冲突加权投票 / 平票处理                        │
│ chain_store.py    内存主链 + 休眠分支（聚合账本与纪元管理器）      │
│ crypto_key.py     ECDSA 区块/交易签名与验签                       │
│ utxo_model.py     UTXO / Transaction 数据模型与交易构造           │
│ utxo_ledger.py    UTXO 账本：奖励、转账、双花检测、余额查询        │
│ epoch_manager.py  纪元编号计算、边界事件、内存快照归档标记         │
└────────────────────────────────────────────────────────────────┘
```

## 5. ✅ 已完成功能清单

### 5.1 后端（Python 单机内存）

- [x] **NovScript 沙箱解释器**：独立 lexer → parser → AST → evaluator 链路；惰性求值 + thunk 记忆化；循环 thunk 检测；不可变 bind；8 类结构化错误（词法/语法/未绑定/类型/参数数量/递归/资源限制/沙箱）；`run_sandbox`、`validate_on_nodes` 对外接口；S-01~S-12 与 F-01~F-18 全套测试。
- [x] **区块数据模型**：不可变 dataclass；SHA256 区块哈希；`canonical_bytes()` 确定性规范化；区块哈希与签名均排除 signature 字段。
- [x] **6 阶段区块校验流水线**：0) ECDSA 签名 → 1) 结构/父区块/高度 → 2) 创世内核兼容性 → 3) mock PoI 词元门槛 → 4) demo/test_cases 解析 → 5) 沙箱运行与预期匹配 → 6) UTXO 交易校验。
- [x] **候选池 + 历史贡献加权投票**：按（父哈希，目标高度）分组；仅主链区块累计 `standard_total_tokens` 作为投票权重；平票时全部候选进入休眠分支。
- [x] **ECDSA 签名**：NIST256p；区块签名与交易签名；签名校验为流水线第 0 步前置检查；篡改内容/公私钥不匹配直接拒绝。
- [x] **UTXO 账本**：挖矿奖励输出、转账、交易签名校验、输入/输出金额约束（允许 0 手续费）、双花检测；休眠/落选区块不修改账本；余额与投票权重完全隔离。
- [x] **纪元基础框架**：`epoch = height // 100` 构造时自动计算并校验；纪元边界（height % 100 == 0）事件检测；内存 EpochSnapshot 与 archived 只读标记；仅主链触发快照。
- [x] **本地 HTTP 服务**：标准库 `http.server`，端口 28417，仅 localhost；启动即初始化创世链并预沉积 102 个合法演示区块。

### 5.2 前端（index.html）

- [x] 地层剖面主视图：垂直滚动、创世内核花岗岩层固定底部、区块即沉积岩层。
- [x] 推演交互：输入提案 → 粒子飘落沉积动画 → 岩层凝聚；成功写入主链。
- [x] 校验失败反馈：岩层碎裂消散动画 + 原因提示（区块进入休眠分支）。
- [x] 岩层详情面板：高度、纪元、矿工署名、描述、演示代码、测试用例。
- [x] 纪元抬升动画：旧纪元地层下沉变半透明归档，新地表浮现。
- [x] 钻探考古：手电筒径向光照照亮地下古岩层。
- [x] 程序画廊：mock 作品列表，代码由真实后端沙箱执行。
- [x] 沙箱弹窗：调用 `/eval-novscript`，展示真实执行结果与中文错误类型。
- [x] 已移除全部硬编码链数据与前端 mock 解释器，数据全部来自真实后端。

### 5.3 测试

- [x] 单元测试共 **72 项全部通过**：
  - NovScript 沙箱：30 项
  - 区块层：13 项
  - 候选池/投票：5 项
  - ECDSA 签名：4 项
  - UTXO 账本：6 项
  - 纪元基础：6 项
  - HTTP API 端到端：8 项

## 6. ⏳ 尚未实现 / 预留未来迭代清单

以下功能在本版本**明确未实现**，仅记录为未来方向，不产生任何虚假承诺：

- [ ] **纪元 LLM 摘要**：多节点并行 LLM 生成纪元摘要 + 摘要投票（当前纪元快照仅含元数据，无摘要内容）。
- [ ] **跨纪元引种提案**：新纪元默认不继承旧纪元特性，引种流程未实现。
- [ ] **大断层事件**：摘要链上限触发与社区投票重置机制未实现。
- [ ] **磁盘持久化**：全部状态在内存，服务关闭即清空。
- [ ] **P2P 网络**：无节点发现、广播、网络投票窗口，无真实分布式共识。
- [ ] **真实 tokenizer / 真实 LLM**：当前仅 mock 词元计数，不调用任何外部模型。
- [ ] **生产级密码学**：ECDSA 仅实验原型；无密钥托管、轮换、撤销、HTTPS。
- [ ] **身份鉴权 / 公网部署 / 数据库**：明确禁止项。
- [ ] **手续费 / 找零自动推导**：当前允许 0 手续费，差额需显式找零输出。
- [ ] **多所有者多签交易**：当前一笔交易要求输入同属一个公钥。
- [ ] **分支升级/切换**：休眠分支目前仅存档，无分支投票升级机制。
- [ ] **UTXO Merkle 结构 / 账本索引**：当前为朴素内存集合。
- [ ] **任何可交易代币或现实金融功能**：永久不在范围内。

## 7. ⚠️ 原型重要限制与风险重申

1. **内存仿真**：服务关闭后所有链数据、账本、快照全部丢失，无恢复手段。
2. **无生产安全性**：沙箱是教学级纯计算隔离，不是生产安全容器；无鉴权、无 HTTPS、无公网部署。
3. **实验固有风险（须如实保留）**：
   - 未来 LLM 摘要存在**信息衰减**与**幻觉风险**；
   - 摘要链仅**线性低速增长**；
   - 共识设计仅适配**小规模仿真网络**；
   - 纯非金融激励存在**参与者留存风险**；
   - 语义校验为关键词/规则级，无法覆盖复杂隐性组合冲突。
4. **无任何可交易资产**：链内 UTXO 凭证仅为实验内部记账，无现实货币价值，禁止交易兑换；外部捐赠池（如有）与链状态严格隔离。
5. **提案→代码映射是简化 mock**：服务端按关键词把提案文本映射为内核语法范围内的演示代码，不代表真实语言设计评审。

## 8. 测试与检查命令

```bash
# 运行全部单元测试（72 项）
python -m unittest discover -s tests -v

# 字节码编译检查
python -m compileall -q *.py tests/*.py

# 运行 HTTP API 测试（会自动占用 28417 端口，先停止手动启动的服务）
python -m unittest tests.test_http_api -v
```

## 9. 后续迭代路线建议

1. **UI 美化**：完善地层质感（SVG 纹理、分层渐晕）、移动端适配、交互音效与性能优化。
2. **mock 纪元摘要模块**：在纪元边界用规则模板生成模拟摘要候选并投票，为未来真实 LLM 摘要预留接口与数据模型。
3. **引种提案原型**：实现旧纪元特性在新纪元的「引种」提案流程（走完整校验）。
4. **演示链更丰富**：增加休眠分支可视化、分支对比视图。
5. **可选**：在明确隔离前提下接入真实本地 LLM（如 Ollama）做提案生成演示。

## 10. 归档包内容索引

| 文档 | 说明 |
|---|---|
| `ARCHIVE_v0_2_README.md` | 本文件：归档总说明与入口 |
| `CHANGELOG_v0_2.md` | 版本变更日志（按开发阶段） |
| `ARCHIVE_FILE_MANIFEST.md` | 完整文件清单（源码/测试/文档/演示） |
| `DEMO_SCRIPT_v0_2.md` | 人工演示操作脚本 |
| `REFERENCE_DOCS_INDEX.md` | 全部历史设计文档索引 |
