# 第 X 章：共识机制与区块完整生命周期

> **章节目标**：补充 StratumGenesis 的共识机制详细规格，并以一个完整示例说明区块从提案生成、PoI 校验、候选冲突处理、投票确认到永久归档的生命周期。
>
> **本章定位**：本章描述小规模实验网络中的协议行为，不将 StratumGenesis 扩展为商用公链，也不引入可交易代币、金融结算或未在全局设计中定义的中心化服务。

## X.1 设计目标与非目标

### X.1.1 设计目标

StratumGenesis 的共识层需要同时满足以下目标：

1. **规则可重放**：节点能够仅依据创世内核、已确认区块及当前纪元规则，独立判断一个候选区块是否合法。
2. **贡献可追溯**：有效提案的作者、标准 token 计数、校验结果和投票结果都被记录，形成可审计的地层历史。
3. **冲突可收敛**：同一位置出现多个合法候选区块时，节点能够依据历史贡献权重选择一个结果，同时保留落选候选及其分支关系。
4. **语言演化受约束**：任何扩展都不能修改 NovScript 创世内核；跨纪元复用旧特性必须通过引种提案。
5. **验证可执行**：提案不仅要通过结构与一致性检查，还必须在确定性的 NovScript 沙箱中完成 demo 或测试运行。
6. **适配小规模网络**：优先保证可理解性、可观察性和可复现性，而不是追求商用公链级吞吐量。

### X.1.2 非目标

本章不定义以下功能：

- 任何可交易、可兑换或具有货币价值的代币；
- 以资产余额为基础的支付、转账或金融结算；
- 对全网参与者身份进行现实世界认证；
- 面向数万节点的高性能拜占庭容错；
- 用摘要替代原始区块；
- 由单一管理员对语言规则进行强制修改。

链内可记录的凭证、贡献记录、投票权重和数字徽章均属于实验系统内部状态，与外部捐赠池严格隔离，不能相互兑换。

## X.2 术语与状态对象

### X.2.1 核心术语

| 术语 | 定义 |
|---|---|
| 创世内核 | NovScript 的永久基础规则，包括惰性求值、纯函数、不可变 `bind` 等已确定的内核约束。 |
| 提案 | 对 NovScript 语言扩展或跨纪元引种的规范化描述，包含语义、示例和可执行校验材料。 |
| 候选区块 | 已由节点本地生成或接收、但尚未完成冲突选择的区块。 |
| 合法区块 | 通过本章定义的结构、签名、PoI、一致性和沙箱校验的候选区块。 |
| 纪元 | 连续的 100 个区块组成的地层阶段。纪元边界触发摘要生成与新纪元初始化流程。 |
| 主链 | 某一语言宇宙中被节点当前采用的连续区块序列。 |
| 分支 | 从某个共同祖先区块分离、并独立记录语言演化的合法区块序列。 |
| 历史贡献权重 | 由已确认历史区块中的标准 token 贡献计算的链内投票权重；它不是余额，也不能转让或交易。 |
| 引种提案 | 将旧纪元特性重新带入新纪元活跃规范的特殊提案。 |

### X.2.2 区块的最小语义字段

实现可以采用不同的物理编码，但逻辑上每个正式区块至少应能表达：

```json
{
  "height": 24,
  "epoch": 0,
  "parent": "<父区块标识>",
  "proposal": {
    "kind": "extension",
    "feature_id": "list-literal",
    "specification": "<语法与语义规范>",
    "examples": ["<NovScript 示例>"],
    "tests": [
      {"program": "<测试程序>", "expected": "<预期结果>"}
    ]
  },
  "poi": {
    "model_metadata": "<仅作记录>",
    "prompt": "<用于生成提案的输入>",
    "output": "<提案生成输出>",
    "standard_tokenizer": "<固定标准分词器标识>",
    "standard_token_count": 412
  },
  "author": "<作者公钥或稳定标识>",
  "signature": "<区块签名>",
  "validation": {
    "consistency": "pass",
    "sandbox": "pass"
  },
  "block_id": "<由规范化内容计算的区块标识>"
}
```

上述结构是逻辑示意，不意味着当前原型已经确定字段名称、签名算法、序列化格式或所有参数。未确定部分必须在原型规范中进一步冻结。

## X.3 区块合法性规则

节点对候选区块执行以下检查。任何一项失败，候选区块都不能进入冲突投票。

### X.3.1 链接与结构检查

节点必须确认：

1. `parent` 指向本节点已知的合法父区块或明确的分支父区块；
2. `height` 等于父区块高度加一；
3. `epoch` 与高度和纪元边界规则一致；
4. 区块字段满足规定的类型、大小和必填约束；
5. 区块标识由规范化字段计算得到，不能由提交者任意声明；
6. 作者签名能够被对应的公开标识验证。

节点不得仅凭展示层文字判断区块内容。所有参与共识的字段必须经过确定性的规范化编码。

### X.3.2 创世内核兼容性检查

候选提案不得：

- 改写或删除 NovScript 的永久内核规则；
- 将惰性求值改为默认严格求值；
- 破坏纯函数约束；
- 使 `bind` 绑定变为可重新赋值；
- 绕过 NovScript 基础语法的可解析边界；
- 引入未经沙箱授权的外部 I/O、网络访问或系统调用。

如果提案声称需要修改上述规则，节点应将其判定为不合法的内核修改，而不是普通语言扩展。该区块不能通过 PoI 或投票被“投票合法化”。

### X.3.3 当前规范一致性检查

节点将候选提案应用于父区块对应的语言规范，检查：

- 特性标识是否重复或与既有定义冲突；
- 语法扩展是否仍可被解释器确定性解析；
- 语义规则是否与已经确认的规则直接矛盾；
- 示例和测试是否引用了尚未激活的特性；
- 引种提案是否明确指向可验证的历史特性及其来源纪元；
- 提案是否把摘要中的不确定描述当作唯一规范来源。

摘要只能作为轻量索引。发生歧义时，节点必须回查永久归档的原始区块；若无法得到一致解释，则该提案不能被视为已验证。

### X.3.4 PoI 校验

PoI 的证明对象是“由本地 LLM 或人类生成、并达到统一标准 token 计数门槛的有效提案材料”，不是无意义哈希，也不是外部货币奖励。

节点应使用协议指定的标准分词器，对规定范围内的提案生成材料重新计数。至少应明确计数范围、编码方式和门槛版本。矿工使用的模型可以不同，但模型名称本身不构成合法性证明。

节点必须拒绝以下情况：

- 标准 token 数低于当前规则门槛；
- 提交材料无法按规定重现计数；
- 计数器版本与当前纪元要求不一致；
- 通过复制无关文本灌满 token，却没有满足提案结构或运行要求；
- 将模型私有 token 统计直接冒充全网标准统计。

PoI 达标不代表提案一定正确。它只是进入语言规则校验和沙箱校验的必要条件之一。

### X.3.5 沙箱校验

节点在确定性 NovScript 沙箱中运行提案携带的 demo 和测试用例。沙箱至少应具备：

- 无文件系统、网络和宿主系统调用；
- 有限的执行步数、内存和输出规模；
- 明确的超时与资源耗尽错误；
- 同一规范、同一输入下可复现的结果；
- 可记录的解释器版本和活跃语言规范标识。

沙箱通过仅说明“给定测试集合在当前实现中得到预期结果”，不能证明该特性在所有程序组合下都没有语义问题。

## X.4 共识流程

### X.4.1 阶段一：提案生成与广播

提案作者从父区块对应的有效语言规范出发，生成扩展提案或引种提案。作者可以使用本地 LLM，也可以人工编写；PoI 记录必须如实标注生成方式。作者将提案、必要的生成材料、标准 token 计数所需内容以及签名封装为候选区块并广播。

广播不等于确认。网络中的其他节点不得因为作者身份、模型品牌或 UI 展示结果而跳过独立校验。

### X.4.2 阶段二：节点本地校验

接收节点按 X.3 节顺序执行本地校验。校验结果至少区分：

- **结构失败**：字段、父区块、签名或编码不合法；
- **规则失败**：与创世内核或当前规范冲突；
- **PoI 失败**：标准 token 计数或计量材料不满足要求；
- **执行失败**：解析失败、测试失败、沙箱超限或结果不确定。

只有全部通过的候选区块才可以进入该父区块高度的合法候选集合。

### X.4.3 阶段三：无冲突追加

如果在规定的收集窗口内，某一高度和父区块下只有一个合法候选区块，节点可以将其追加为下一层地层。追加时应记录确认所依据的父区块和本地校验结果。

“只有一个候选”不应被理解为“该区块绝对正确”。它只表示在当前节点可见网络和规则下没有发现同高度的其他合法候选。

### X.4.4 阶段四：合法冲突投票

如果同一父区块下出现两个或多个均通过本地校验的候选区块，则它们构成合法冲突。节点在确定的投票窗口内选择一个候选，并按照历史贡献权重统计结果。

投票权重满足以下边界：

1. 权重只能来自已确认历史贡献，不能来自当前尚未确认的候选区块；
2. 权重不能通过链内转账、购买或外部付款转移；
3. 同一贡献不能在多个身份间重复计算；
4. 计权规则必须对所有候选一致，不能针对某一特性临时调整；
5. 计票输入、窗口和结果应可审计。

获得最高有效权重的候选区块成为该父区块下的选择结果。若出现权重完全相同、投票窗口不一致、候选集合不一致等情况，节点不得擅自猜测结果，应进入协议规定的平局或重同步处理；这些处理方式在当前版本仍属于开放工程问题。

### X.4.5 阶段五：落选区块与分支保存

落选区块不得被当作不存在。节点应保存其原始内容、校验结果、投票结果及与获选区块的分叉关系，形成可考古的平行语言宇宙。

落选区块默认不改变当前主链的活跃语言规范。只有在某条分支上继续形成了明确的分支链，并依据该分支的采用规则被节点选择时，分支才会成为该语言宇宙的当前演化路径。当前版本不把“所有落选区块自动合并回主链”作为有效行为。

## X.5 区块完整生命周期示例

以下示例不构成产品承诺，只用于说明协议顺序。假定网络处于纪元 0，高度 23，父区块中的创世内核和既有扩展均已可重放。

### X.5.1 生成候选

参与者 Alice 想为 NovScript 提交列表字面量扩展。她使用本地模型生成提案，也可以对模型输出进行人工修订。最终提案包含：

```novscript
(bind xs [1, 2, 3])
(head xs)
```

以及对应测试：

```text
输入： (head [1, 2, 3])
预期： 1
```

提案声明该扩展遵守惰性求值和不可变绑定，不修改创世内核。Alice 将提案材料、模型元数据、标准 token 计数材料和作者签名封装成高度 24 的候选区块。

### X.5.2 本地校验

节点收到候选后依次确认：

1. 父区块是本节点认可的高度 23 区块；
2. 高度、纪元和区块标识正确；
3. 作者签名有效；
4. 提案没有声明内核修改；
5. 标准分词器重新计数达到当前 PoI 门槛；
6. 解析器能解析列表语法；
7. 沙箱运行测试得到 `1`，且未触发资源限制；
8. 提案与当前活跃规范不冲突。

通过后，Alice 的区块进入高度 24 的合法候选集合。

### X.5.3 同高度冲突

在同一收集窗口内，Bob 也提交了高度 24 的合法字符串扩展。两个区块都以高度 23 为父区块，且都通过独立校验，因此不能简单按“先收到者”认定结果。

节点按照已确认历史记录计算 Alice 和 Bob 的历史贡献权重，在投票窗口结束后统计候选票数。假设 Alice 的列表提案获得最高有效权重，则列表扩展成为高度 24 的主链区块。

Bob 的字符串区块仍被保存为落选分支节点。它可以被查看、重放和作为平行语言宇宙的起点，但不会自动成为高度 24 主链的活跃特性。

### X.5.4 确认后的地层状态

高度 24 区块确认后：

- 列表扩展加入主链对应的活跃语言规范；
- Alice 获得链内贡献记录、投票权重累计和永久地层署名；
- 不产生可交易代币或现金奖励；
- 节点保存区块原文、校验结果、投票证据和分支关系；
- 后续提案可以引用列表特性，但必须以该高度的规范状态为依据。

### X.5.5 纪元边界

当主链累计完成 100 个区块时，当前纪元进入结束流程：

1. 该纪元的原始区块永久归档并保持只读；
2. 多个节点并行生成纪元摘要；
3. 节点对摘要候选进行选择，形成轻量索引；
4. 新纪元开始时，旧特性不自动成为新纪元活跃特性；
5. 若要继续使用列表扩展，参与者必须提交引种提案，并再次经过完整 PoI、一致性和沙箱校验。

摘要不能覆盖或改写原始区块。若摘要与原始区块冲突，原始区块是审计依据，摘要只能被视为错误或不完整的索引。

## X.6 边界约束

### X.6.1 创世内核边界

创世内核是协议的不可变身份锚点。任何“通过投票即可修改内核”的实现都违反本白皮书。协议升级如果未来确有必要，必须作为实验范围外的全新版本或新语言宇宙处理，不能伪装成普通扩展区块。

### X.6.2 纪元边界

“每 100 区块一个纪元”是地层组织规则，不是自动继承规则。纪元结束只触发归档、摘要和新活跃规范初始化，不意味着旧特性永久消失，也不意味着旧特性自动加载。

### X.6.3 摘要边界

摘要只能降低日常上下文负担，不能作为无损规范存储。节点在涉及底层兼容性、争议校验或引种提案时，应允许回查原始区块。

### X.6.4 沙箱边界

沙箱只负责在有限资源和指定输入下执行代码。它不能证明任意程序都正确，也不能阻止所有由语言语义组合产生的隐性问题。解释器若无法确定性重放，候选区块应视为校验失败或待处理状态，而不是强行确认。

### X.6.5 网络规模边界

本机制面向小规模分布式实验网络。候选广播、全量规范校验、摘要投票和历史贡献计权都会随节点数、区块数和语言复杂度增加而变重。当前设计不承诺数万节点、公网级延迟或工业级可用性。

## X.7 失败场景与处理原则

| 失败场景 | 节点行为 | 是否进入主链 |
|---|---|---|
| 父区块未知或高度不连续 | 暂存为待同步对象，不能验证 | 否 |
| 签名或区块标识错误 | 拒绝候选并记录失败原因 | 否 |
| 修改创世内核 | 直接判定规则失败 | 否 |
| 标准 token 未达门槛 | PoI 失败 | 否 |
| 示例无法解析 | 执行前失败 | 否 |
| 沙箱输出与预期不一致 | 执行失败 | 否 |
| 沙箱超时或超出资源限制 | 执行失败，不得无限等待 | 否 |
| 与当前规范冲突 | 一致性失败 | 否 |
| 多个候选均合法 | 进入加权投票 | 仅获选者进入该主链高度 |
| 投票结果平局或候选集合不一致 | 按待确认的平局/重同步规则处理 | 未决时不得擅自确认 |
| 纪元摘要与原始区块冲突 | 以原始区块为审计依据，标记摘要异常 | 摘要不能改写主链 |
| 引种提案失败 | 不激活对应旧特性，可重新提交新提案 | 否 |
| 节点无法完成确定性重放 | 标记为不可验证，不应投票确认 | 否 |

失败区块及失败原因是否永久写入公共审计记录，需要在原型阶段明确数据格式；但无论采用何种记录方式，失败不能被解释为成功贡献。

## X.8 风险说明

### X.8.1 历史贡献权重的集中风险

历史 token 贡献权重可能逐渐集中在少数高算力节点或少数长期参与者手中。虽然系统不允许购买投票权重，但这仍可能造成语言方向上的事实集中。权重分布、节点数量和投票参与率应作为实验观测指标，而不是被宣称为已解决的问题。

### X.8.2 LLM 同质化风险

多个节点可能使用同一模型、相似提示词和相同训练数据，从而在摘要或提案校验中产生相同幻觉。多节点投票不等于独立事实证明；如果输入和模型高度同质，错误也可能形成多数。

### X.8.3 语义组合爆炸

单个测试用例通过，不代表新特性与所有既有特性的组合都安全。随着语言扩展增加，完整交互测试的成本可能超过小规模节点的承受能力，导致隐性冲突延迟暴露。

### X.8.4 分支碎片化风险

落选区块永久保存有利于研究语言分化，但也可能形成大量无人维护的平行分支。分支越多，规范、解释器状态和测试矩阵越难维护，用户也可能难以判断应使用哪个语言宇宙。

### X.8.5 摘要信息衰减风险

摘要存在信息衰减、错误概括和幻觉风险。原始区块永久归档只能提供纠错依据，不能保证所有参与者都会主动回查原文，也不能阻止错误摘要影响后续提案生成。

### X.8.6 非金融激励的留存风险

永久署名、链内投票权重和数字徽章不一定足以支撑长期参与。参与热度下降会直接降低提案数量、投票覆盖率和测试质量。外部可选捐赠池只能提供与链内凭证隔离的资源支持，不能被视为稳定的经济激励模型。

### X.8.7 小网络适用性风险

当网络规模超过设计范围时，广播、全量验证和加权投票可能出现延迟、视图不一致或参与不足。StratumGenesis 应将规模上限作为实验边界公开记录，而不是在没有验证的情况下宣称具备公链扩展能力。

## X.9 【开放问题】

本章仍待确认以下工程问题：

1. **区块规范化编码**：采用何种确定性序列化格式，如何避免不同实现对同一提案计算出不同区块标识？
2. **签名与身份标识**：作者公钥格式、签名算法、密钥轮换和丢失密钥后的处理方式是什么？
3. **PoI 计数范围**：标准 token 计数究竟覆盖提示词、模型输出、人工修订内容中的哪些部分？如何防止无关文本灌水？
4. **标准分词器版本**：标准分词器由谁冻结、如何随协议版本记录、旧纪元区块如何重放？
5. **候选收集窗口**：网络如何判断一个高度的候选收集窗口开始和结束？网络延迟下如何避免不同节点看到不同候选集合？
6. **投票权重快照**：冲突投票使用父区块时刻、投票窗口开始时，还是其他明确时点的历史贡献权重？
7. **平票处理**：候选权重相同或投票结果不完整时，是否重投、延长窗口，还是生成明确的分支？
8. **离线节点与重连**：节点长期离线后如何同步主链、落选分支、摘要候选及投票证据？
9. **分支选择规则**：落选分支在什么可验证条件下可以成为活跃分支？“累计贡献”“节点支持”和“语言采用率”各自如何定义？
10. **摘要投票的独立性**：如何记录摘要生成所依据的原始区块范围，并降低同模型同提示词造成的相关性错误？
11. **引种提案的依赖闭包**：一个旧特性依赖多个更早特性时，引种提案是否必须同时声明并验证完整依赖集合？
12. **解释器版本一致性**：沙箱解释器本身如何版本化，解释器修复与语言扩展如何区分，如何保证历史区块可重放？
13. **资源限制参数**：执行步数、内存、输出长度和超时上限的默认值及其纪元内变更规则是什么？
14. **失败记录格式**：失败候选是否永久归档，归档内容的最小字段和隐私边界是什么？
15. **网络规模阈值**：以何种观测指标判定当前共识机制已超出“小规模实验网络”的适用范围？

在上述问题冻结前，本章中的相关内容应视为协议设计约束与待实现规范，而不是已经完成的生产功能。
