# StratumGenesis 实验指标口径说明（并行线 C）

> 对应设计白皮书 §13（学术价值）与 §14.5（长期：定期导出完整链数据、撰写实验报告）。
> 本文档只描述 `analyze_chain.py` 的指标口径与边界，**不改动任何既有文件的语义**。

---

## 1. 这份文档解决什么问题

StratumGenesis 的「语言演化实验」过去只靠人眼读存档。存档会越来越大，而「纪元 1 到底
比纪元 0 多激活了哪些原语」「哪些原语在这一纪元丢了」这类问题，手工数一遍既不严谨也
不可复现。本线交付的 `analyze_chain.py` 把存档变成一组**可直接引用、可反复跑、键名稳定**
的指标，让 §14.5 所说的「定期导出 + 撰写实验报告」有了机器可读的底座。

它的定位很克制：这是一个**只读的离线分析器**。不启动 HTTP 服务，不写 `data/`，不联网，
也不改动 `server.py`、`persistence.py`、`chain_store.py` 等任何既有文件。
它只读一份 JSON，然后算。唯一的第三方依赖是项目既有的 `ecdsa`（本线不新增任何库）。

## 2. 运行方式与数据来源

```
python analyze_chain.py <存档或导出 JSON> [--verify] [--json <输出>] [--compact]
python analyze_chain.py --synthetic [--json <输出>]      # 内存合成链演示
python analyze_chain.py [--json <输出>]                  # 无存档：提示 + 合成链，退出码 0
```

- `--verify` 与 `--verify-hashes` 是**等价旗标**（后者为别名，为兼容既有文档与
  自动化脚本保留）。两者的行为完全一致：调用 `persistence.load_state` 做逐块
  哈希 + 签名重验，通过 exit 0，未通过 exit 1。分析器**没有** `--verify-hashes`
  之外的其他变体，`--help`、本脚本 docstring 与本文档三处一致。
- **无位置参数 = 已批准的演示路径，不是「参数被忽略」**。省略存档路径时，
  分析器打印一行中文提示，改用内存合成链（跨 3 个纪元）跑完整指标，**退出码仍为 0**。
  这是被明确接受的行为；自动化调用方如果依赖退出码区分「跑真实存档」与「跑演示」，
  请检查输出里的 `source.label`（`"合成链（无存档模式）"`）与 `source.path`
  （`"(内存合成链)"`），不要仅凭退出码判断。

两条数据来源，都是只读：

1. **存档/导出 JSON（主路径）**。由 `persistence.load_state()` 读取的 chain-v2 格式。
   生成一份新的存档而不动 `data/`：
   ```
   python server.py --archive <临时路径> --fresh        # 预沉积 102 块并落盘
   python server.py --archive <路径> --export <导出路径>  # 导出
   ```
   注意 `data/chain_v1.json` 是旧格式样本，加载会被**正确拒绝**（版本不匹配），
   不要试图读它、更不要删它。

2. **内存合成链（`--synthetic`）**。跨 3 个纪元的合成主链，走既有
   `block_model` / `chain_store` / `crypto_key` / `mock_tokenizer` /
   `persistence.serialize_state` 的真实追加路径（含纪元首块重置），因此生成的 JSON
   与服务器导出的 chain-v2 格式完全一致。它的用途是演示「引种 / 失忆」这类
   预沉积链里观测不到的指标——预沉积链没有激活事件。

两个细节值得先说清楚，因为它们直接影响你读到什么：

- **预沉积链的 `activation` 字段全为空**。这是真实特征，不是分析器读错了字段名
  （已核对 `blocks[].activation` 的键名与取值）。预沉积是「造块」而非「做语言实验」，
  所以它的语言演化指标全部为零值。要看引种与失忆，请用 `--synthetic`。
- **创世块不发奖励**。`persistence.rebuild_store` 显式跳过 `height == 0`（创世块已由
  `ChainStore` 初始化），所以实际发放奖励的块数 = 主链块数 − 1。分析器按此口径计算，
  因此「已发放奖励」与「总发行量」与账本余额严格对账一致。创世块的矿工也不在
  `miners` 注册表里，报告中会标注 `[含创世块 1、不在 miners 注册表]`。

## 3. 依赖边界：哪些字段被读，哪些被刻意避开

分析器只从这些字段推导（`field_provenance.derived_from`）：

| 指标类别 | 来源字段 |
|---|---|
| 语言演化 | `blocks[].activation`、`blocks[].height` |
| 链与共识 | 上述 + `blocks[].miner_pubkey_b64`、`blocks[].block_hash`、`sleeping_branches[].branch_id`、`sleeping_branches[].blocks[]`、`rejection_reasons`、`miners[]` |
| 账本 | `blocks[]` 全量（经 `persistence.rebuild_store` 确定性重放）+ `miners[]` |
| PoI | `blocks[].poi.standard_total_tokens`、`standard_input_tokens`、`standard_output_tokens`、`standard_tokenizer` |

**刻意避开**的字段（`field_provenance.avoided_fields`，因为语义正在变更中）：

- `epochs[].active_features` —— 不读。纪元内的激活情况完全从 `blocks[].activation` 推导。
- `current_active_features` —— 不读。
- `language_features` —— 不读。
- `blocks[].epoch` —— 不信任。纪元一律按 `epoch = height // 100` 重新推导
  （与 `epoch_manager.EPOCH_BLOCKS` 一致），存档里的值只是参考。
- `blocks[].proposal.kind` / `proposal.feature_id` —— 已解析但未纳入任何指标。
- `summary_chain`、`miner_label`、`reason` —— 不读。

这个边界是可验证的，不是口头承诺：测试里往存档注入了 5 个 `SENTINEL_*` 哨兵值，
断言它们不会出现在任何报告字段里；同时把所有块的 `epoch` 故意写成 `999`，
断言分析器仍按 `height // 100` 归入正确的 3 个纪元。

---

## 4. 指标定义与计算口径

### 4.1 语言演化指标

| 指标 | 定义与口径 | 来源字段 |
|---|---|---|
| 首次激活高度 | 每个原语在整条主链上第一次出现在 `activation` 里的高度，附所属纪元与提案者 | `blocks[].activation` + `blocks[].height` |
| 新激活 | 该纪元内首次出现、且在**更早纪元从未出现过**的原语 | 同上 |
| 引种 | 该纪元内出现、且在**更早纪元的 `blocks[].activation` 中出现过**的原语 | 同上 |
| 开篇可用原语集 | 该纪元**第一个含 activation 的块**的 activation 内容 | 同上 |
| 失忆集合 | `上一纪元末累计可用集 − 本纪元开篇可用集`；纪元 0 恒为空 | 同上 |
| 累计历史可用集 | 所有纪元「新激活」的并集，只增不减（历史层 provenance） | 同上 |
| 激活事件总数 | 所有块 `activation` 长度之和 | 同上 |

推导是单遍扫描：先按 `height // 100` 分组，再在每个纪元内按高度升序遍历。
「开篇可用」取该纪元第一处激活——因为纪元作用域下语言从空起步，只有纪元首块（或该纪元
首个激活块）的 activation 能在纪元开篇生效。一个原语在同一纪元内多次出现只计入一次
（用 `seen_in_epoch` 去重），但会累计到激活事件总数。

### 4.2 链与共识指标

| 指标 | 定义与口径 | 来源字段 |
|---|---|---|
| 总高度 / 主链区块数 | 最后一个区块的 height；`blocks` 长度（含创世块） | `blocks[].height` |
| 每纪元区块数 | 按 `height // 100` 分组的块数，附期望值 100 与是否「满」 | 同上 |
| 休眠分支数 / 各自高度 | `sleeping_branches` 条目数；每条分支的块数、min/max 高度、覆盖纪元、激活原语 | `sleeping_branches[].*` |
| 提案成功率 | 提交 = 主链非创世块数 + 休眠分支条目数；主链接受 = 主链非创世块数；落选 = 休眠分支条目数 | `blocks[]`、`sleeping_branches[]` |
| 矿工区块数与累计工作量 | 按 `miner_pubkey_b64` 分组：块数、累计 `standard_total_tokens`（PoI 工作量）、占比、含激活块数、是否含创世块、是否在 `miners` 注册表 | `blocks[].*`、`miners[]` |
| 投票权重分布 | 仅主链计入，按 `sum(poi.standard_total_tokens)` 分组：总量、头名占比、p50/p90/max/min | 同上 |
| 拒绝原因分布 | `rejection_reasons` 值计数 | `rejection_reasons` |

投票权重分布的 min/max 只统计**有产出**（> 0）的矿工，避免零权重的创世块矿工污染分位数。

提案成功率里有一个刻意选择：比率分母是「提案数」而非「块数」，休眠分支按**条目**计
（每条休眠分支 = 一次被否决的提案线），不按块数计。因为同一条落选线可能带多个区块，
按块数计会把落选严重度放大。同时保留 `branch_blocks_total` 以便核对绝对量。

比率统一用 `_rate(n, d)`：分母为 0 返回 `None`（表示「无样本」），而不是 0%，
空链与仅创世块两种情况都走这条路。

### 4.3 账本指标

| 指标 | 定义与口径 | 来源字段 |
|---|---|---|
| 各矿工 UTXO 余额 | 从主链区块确定性重放得到 | `blocks[]` |
| 总发行量 | 余额总和（等价于「发放 − 消费」） | 同上 |
| 交易笔数 | 所有块 `transactions` 长度之和 | `blocks[].transactions` |
| 未花费 UTXO 数 | 账本快照条目数 | 同上 |

账本**不直接序列化在存档里**——`persistence.py` 明确说明账本与纪元快照是按既有规则
从主链确定性重建的。所以分析器复用 `persistence.rebuild_store` 这个同一入口，
得到的余额与服务器加载存档时完全一致。

降级路径：如果重放失败（例如最小 JSON 输入缺少可重建所需的完整字段），
分析器不报错退出，而是回退到「发放 − 消费」估算，并在 `warnings` 与 `method`
里如实标注（`derived-fallback`），余额字段留空。报告不会假装账本是完整的。

### 4.4 PoI 工作量指标

| 指标 | 定义与口径 | 来源字段 |
|---|---|---|
| 词元总量 / 均值 / 中位数 / 最大 / 最小 | 全部主链块（含创世块）的 `standard_total_tokens` | `blocks[].poi.*` |
| p25 / p75 / p90 | 线性插值分位数（`_percentile`） | 同上 |
| 零词元区块数 | `standard_total_tokens == 0` 的块数（创世块恒为 0） | 同上 |
| 仅主链（剔除创世块） | `height > 0` 子集的块数、总量、均值 | 同上 |
| 输入/输出词元合计 | `standard_input_tokens` / `standard_output_tokens` 求和 | 同上 |
| 分词器标识分布 | 按 `standard_tokenizer` 分组的块数，及其中含词元的块数 | 同上 |

---

## 5. 关于「引种 / 失忆」的语义阶段标注 ⚠️

**这是本线最重要的限制，请务必先读这一段。**

「引种（inoculation）」与「失忆（lost memory）」这两个指标，在**阶段 D（纪元作用域 +
引种提案）落地之前，不是权威语义**。

原因：真正的「引种」是一个**协议行为**——某个矿工在纪元开篇主动声明「把上一个纪元
用过的原语带回来」。那是一个有名字的、被投票的、可能落选的动作。而阶段 D 之前，
链上并不存在这个动作，`activation` 字段里也分辨不出「这是引种」还是「这是新激活」。

所以在阶段 D 之前，本分析器用一条**临时推导口径**：

> 「引种」= 该原语在更早纪元的 `blocks[].activation` 中出现过。

这条口径的已知偏差：

- 它**看不出提案是否被否决**。真实语义里落选进入休眠分支的引种不算「已引种」，
  而临时口径只看主链，会把落选的引种一并排除（因为休眠分支不进入主链扫描）。
  这在多数情况下方向是对的，但原因不是分析器理解协议，而是它只看了主链。
- 它**把「恰好被重新激活」当成「主动引种」**。一个矿工在纪元 1 重新激活 `-`，
  在临时口径下记为引种；但它可能只是被随机轮换选到，与「有意带回上一纪元语言」无关。
- 「开篇可用原语集」同样是从第一个激活块**反推**出来的，而不是纪元开篇的
  **实际作用域状态**。阶段 D 之后，正确口径应当读纪元作用域的首块重置结果。

落地阶段 D 之后，请重新核对这三个指标的语义定义，并在 `EXPERIMENT_METRICS.md`
更新本节。在此之前，凡引用「引种事件数」或「失忆集合」，都应带上
「阶段 D 前临时口径」这个限定语。

其余指标（首次激活高度、区块数、提案成功率、账本、PoI）不受阶段 D 影响，
口径现在是稳定的。

---

## 6. 数据发布建议（白皮书 §14.5）

**导出格式。** 用 `persistence.export_chain` 产出的 chain-v2 JSON 作为发布格式，
不要另造格式。它是自包含的：`blocks` + `sleeping_branches` + `miners` +
`rejection_reasons`，加上存档时缺失但可按同一规则重建的账本。分析器读取它
不需要访问服务器。

**字段稳定性。** 发布链上依赖的字段只有第 3 节表格里那几列。如果某条并行线打算
改动 `blocks[].activation`、`height`、`poi.standard_*` 的语义或类型，请同步
更新本文件第 3 节，并留意 `tests/test_analyze_chain.py` 里的依赖边界断言。
建议把 `format_version` 的判定作为发布门禁：分析器期望 `chain-v2`，
遇到其他版本会明确报错并拒绝分析，不会静默产出一堆错的数。

**建议的发布字段。** 除了原始 chain-v2 JSON，建议同时发布
`analyze_chain.py <导出> --json metrics.json` 的结果。机器可读结构里
`schema.schema_version` 是分析器自身版本（当前 `1`），便于区分「链变了」和「指标口径变了」。
键名一旦对外发布就不要改，只能新增。

**已知局限（请在发布说明里如实注明）：**

- **无真实 LLM**。所有 PoI 词元来自 `mock_tokenizer`，是确定性 mock 计数，
  不代表任何真实模型的工作量分布。
- **无真实网络**。单进程仿真实验，矿工是本地轮换的密钥对，不存在真实的
  竞争、延迟或恶意行为。
- **样本量小**。预沉积链 102 块、合成链 205 块，且预沉积链没有任何激活事件。
  任何统计结论都需要更大的运行批次支撑。
- **创世块工矿用占位密钥**，不在 `miners` 注册表内，报告中会单独标注。
- **签名重验统一走 `crypto_key.verify_block_signature`**。`--verify` 只是入口选择器，
  逐块哈希与 ECDSA 签名由 `persistence.load_state` 内部调用项目既有的
  `crypto_key.verify_block_signature` 完成——与服务器加载存档是同一个实现来源。
  本脚本**不内嵌任何手写 ECDSA / P-256 实现**，也不做 `sys.modules` 注入。
- **缺少 ecdsa 依赖时明确跳过并标注**。若运行环境没有 `ecdsa`，`--verify` 打印
  「本机缺少 ecdsa 依赖，跳过签名重验」，并在 `--json` 里标记
  `schema.ecdsa_backend = "unavailable"`、`schema.signature_verification = "skipped_no_ecdsa"`，
  同时把这条说明写进 `warnings`。分析继续执行、退出码仍为 0；**绝不用手写实现冒充真实验签**。
  ecdsa 可用时 `ecdsa_backend = "python-ecdsa"`、`signature_verification = "enabled"`。

---

## 7. 运行示例

### 7.1 合成链模式（演示引种与失忆）

```
$ python analyze_chain.py --synthetic
```

```text
==============================================================================
StratumGenesis · 离线链分析 / 实验指标报告
==============================================================================
数据来源      : 合成链（无存档模式） ((内存合成链))
格式版本      : chain-v2  | 纪元粒度 = height // 100
主链总高度    : 205  | 主链区块数 = 206  | 覆盖纪元数 = 3
ECDSA 后端    : python-ecdsa

------------------------------------------------------------------------------
① 语言演化指标
------------------------------------------------------------------------------
历史层累计激活原语（provenance，只增不减）：*, -, head, list
首次激活原语数 = 4  | 总激活事件 = 5  | 引种事件 = 1  | 失忆原语数 = 6

  原语首次激活：
    *        height=101   epoch=1  proposer=矿工·B
    -        height=1     epoch=0  proposer=矿工·A
    head     height=2     epoch=0  proposer=矿工·B
    list     height=2     epoch=0  proposer=矿工·B

  逐纪元演化（开篇可用 / 新激活 / 引种 / 失忆）：
    纪元 0  (height 0~99, 区块 100 块)
      开篇可用原语 : -
      新激活       : -, head, list   (3 个)
      引种         : —   (0 个)
      失忆集合     : —   (0 个)
    纪元 1  (height 100~199, 区块 100 块)
      开篇可用原语 : -
      新激活       : *   (1 个)
      引种         : -   (1 个)
      失忆集合     : head, list   (2 个)
    纪元 2  (height 200~205, 区块 6 块)
      开篇可用原语 : —
      新激活       : —   (0 个)
      引种         : —   (0 个)
      失忆集合     : *, -, head, list   (4 个)
```

这张表就是合成链设计的意图：纪元 0 从零起步激活三个原语（全部「新激活」）；
纪元 1 开篇引种了 `-`，但又丢了 `head` 与 `list`（失忆 2 个），随后新激活 `*`；
纪元 2 开篇完全不引种，四个原语全部失忆——这就是「失忆」指标要捕捉的场景。

```text
------------------------------------------------------------------------------
② 链与共识指标
------------------------------------------------------------------------------
  每纪元区块数：
    纪元 0     100 / 100  [满]  激活事件 3
    纪元 1     100 / 100  [满]  激活事件 2
    纪元 2       6 / 100  [未满]  激活事件 0

  休眠分支数 = 1  | 休眠分支区块总数 = 1
    分支 3fd350ce77b373ca…  1 块  height 206~206  激活=-
      拒绝原因：冲突投票落选，保存为休眠分支

  提案成功率：主链 205/206 = 99.5%  | 休眠分支 0.5%
      落选入分支提案 = 1 个

  矿工区块数与累计工作量（PoI），共 4 个矿工：
    矿工·A       区块   69 块  累计 token      2,081  (占比 33.6%)  含激活 2 块
    矿工·B       区块   68 块  累计 token      2,069  (占比 33.4%)  含激活 2 块
    矿工·C       区块   68 块  累计 token      2,040  (占比 33.0%)  含激活 0 块
    矿工(Z2VuZXNp…) 区块    1 块  累计 token          0  (占比 0.0%)  含激活 0 块  [含创世块 1、不在 miners 注册表]

  投票权重分布（仅主链计入，总量 6,190 token，有产出矿工 3 个）：
    头名占比 = 33.6%  | p50 = 2,069  | p90 = 2,079  | max = 2,081  | min = 2,040
  拒绝原因分布：
       1 x 冲突投票落选，保存为休眠分支

------------------------------------------------------------------------------
③ 账本指标
------------------------------------------------------------------------------
  口径：replayed-via-persistence.rebuild_store（与存档加载同一重建路径）
  每块奖励 = 100  | 已发放奖励 = 20,500  | 交易消费 = 0  | 总发行量 = 20,500
  未花费 UTXO 数 = 205  | 交易笔数 = 0
  各矿工 UTXO 余额：
    矿工·A       余额    6,900  (未花费 69 笔)  公钥 0026106933dd…
    矿工·B       余额    6,800  (未花费 68 笔)  公钥 6d6ee0510f92…
    矿工·C       余额    6,800  (未花费 68 笔)  公钥 e83ed7f12ddf…

------------------------------------------------------------------------------
④ PoI 工作量指标
------------------------------------------------------------------------------
  词元总量 = 6,190  | 均值 = 30.0  | 中位数 = 30  | 最大 = 46  | 最小 = 0
  p25 = 30  | p75 = 30  | p90 = 30  | 零词元区块 = 1
  仅主链（剔除创世块）：205 块  总量 6,190  均值 30.2
  输入词元合计 = 3,706  | 输出词元合计 = 2,484
  分词器标识分布：
    mock-tokenizer-v1     205 块（其中 205 块含词元）
    mock-v1                 1 块（其中 0 块含词元）
```

对账检查：`已发放奖励 20,500 = 总发行量 20,500 = 6,900 + 6,800 + 6,800`，
且每位矿工余额 = 块数 × 100（69/68/68 × 100），账本与共识两侧完全自洽。

（注：合成链每次运行的随机密钥与哈希会变化，分支 ID、公钥前缀等会不同，
但所有计数与比率是确定的。）

### 7.2 预沉积链（真实的 chain-v2 导出）

```
$ python analyze_chain.py <导出 JSON> --verify
[校验] 逐块哈希与 ECDSA 签名重验通过：103 个主链区块 + 0 个休眠分支区块
```

```text
==============================================================================
StratumGenesis · 离线链分析 / 实验指标报告
==============================================================================
数据来源      : 存档/导出 JSON (…/exported.json)
格式版本      : chain-v2  | 纪元粒度 = height // 100
主链总高度    : 102  | 主链区块数 = 103  | 覆盖纪元数 = 2
ECDSA 后端    : python-ecdsa

------------------------------------------------------------------------------
① 语言演化指标
------------------------------------------------------------------------------
历史层累计激活原语（provenance，只增不减）：—
首次激活原语数 = 0  | 总激活事件 = 0  | 引种事件 = 0  | 失忆原语数 = 0
    —（无激活事件）
    纪元 0  (height 0~99, 区块 100 块)   新激活 0 / 引种 0 / 失忆 0
    纪元 1  (height 100~102, 区块 3 块)  新激活 0 / 引种 0 / 失忆 0

------------------------------------------------------------------------------
② 链与共识指标
------------------------------------------------------------------------------
  每纪元区块数：
    纪元 0     100 / 100  [满]  激活事件 0
    纪元 1       3 / 100  [未满]  激活事件 0

  休眠分支数 = 0  | 休眠分支区块总数 = 0
  提案成功率：主链 102/102 = 100.0%  | 休眠分支 无样本

  矿工区块数与累计工作量（PoI），共 4 个矿工：
    化石刻工·临渊    区块   34 块  累计 token      2,941  (占比 37.8%)  含激活 0 块
    地层匠·沧石     区块   34 块  累计 token      2,482  (占比 31.9%)  含激活 0 块
    沉积者·阿砚     区块   34 块  累计 token      2,363  (占比 30.3%)  含激活 0 块
    矿工(Z2VuZXNp…) 区块    1 块  累计 token          0  (占比 0.0%)  含激活 0 块  [含创世块 1、不在 miners 注册表]

  投票权重分布（仅主链计入，总量 7,786 token，有产出矿工 3 个）：
    头名占比 = 37.8%  | p50 = 2,482  | p90 = 2,849  | max = 2,941  | min = 2,363

------------------------------------------------------------------------------
③ 账本指标
------------------------------------------------------------------------------
  每块奖励 = 100  | 已发放奖励 = 10,200  | 交易消费 = 0  | 总发行量 = 10,200
  未花费 UTXO 数 = 102  | 交易笔数 = 0
    化石刻工·临渊    余额    3,400  (未花费 34 笔)
    地层匠·沧石     余额    3,400  (未花费 34 笔)
    沉积者·阿砚     余额    3,400  (未花费 34 笔)

------------------------------------------------------------------------------
④ PoI 工作量指标
------------------------------------------------------------------------------
  词元总量 = 7,786  | 均值 = 75.6  | 中位数 = 71  | 最大 = 89  | 最小 = 0
  p25 = 70  | p75 = 84  | p90 = 89  | 零词元区块 = 1
  仅主链（剔除创世块）：102 块  总量 7,786  均值 76.3
  输入词元合计 = 3,094  | 输出词元合计 = 4,692
```

注意语言演化部分全是零值——如第 2 节所述，这是预沉积链的真实特征（它的
`blocks[].activation` 全为空数组），不是分析器的读取错误。这类存档的价值在于
链与共识、账本、PoI 三组指标，而不是语言演化。

---

## 8. 未做与已知局限

- **未实现真正的引种提案语义**。当前「引种」是从 `blocks[].activation` 的历史出现
  推导的，见第 5 节的完整说明。
- **休眠分支与拒绝原因的映射是尽力而为**。分析器用 `branch_id`（即 `parent_hash`）
  去查 `rejection_reasons`；如果存档里的原因键用了别的口径，该项会显示为空字符串，
  不影响其他指标。
- **账本不覆盖休眠分支**。`persistence.rebuild_store` 只重放主链（休眠分支不触碰
  账本），这是既有实现的设计，分析器遵循它。
- **分析器默认不重算区块哈希与签名**。默认只做结构与版本校验；
  需要逐块重验时请显式加 `--verify`（会调用 `persistence.load_state`，
  内部走 `crypto_key.verify_block_signature`）。缺 `ecdsa` 时该项被跳过并标注，
  见第 6 节。
- **`--verify` 抓不到篡改 `signature_bytes_b64` 之外的哈希级篡改**。签名校验补上了
  `block_hash` 不含签名带来的盲区：只改签名的存档能通过哈希校验，但签名重验会失败。
  已实测：篡改 height=42 的签名后 `--verify` 报「签名校验失败（height=42）」并 exit 1。
- **无真实 LLM、无真实网络、样本量小**，详见第 6 节。
- **未生成图表**。`--json` 输出键名已保持稳定（`schema.schema_version = 1`），
  便于后续接画图工具，但本线不做可视化。

---

## 9. 新增文件清单

| 文件 | 用途 |
|---|---|
| `analyze_chain.py` | 离线链分析器（CLI + 指标计算 + 报告渲染） |
| `EXPERIMENT_METRICS.md` | 本文档：指标定义、计算口径、来源字段、运行示例、发布建议 |
| `tests/test_analyze_chain.py` | 37 个测试用例，覆盖合成链指标、边界、错误处理、依赖边界、`--verify` 与篡改检测、缺依赖跳过、无参数回退 |

未修改任何既有文件；未执行任何 git add / commit；未改动或删除 `data/` 与
`data/chain_v1.json`；未启动 HTTP 服务、未占用端口 28417。
