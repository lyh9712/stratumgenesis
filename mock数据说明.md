# StratumGenesis 静态 UI 原型 · Mock 数据说明

## 定位声明

`index.html` 是**纯静态前端原型**：

- 不连接 Python StratumGenesis 项目（novscript 沙箱、区块层、投票、UTXO 均不参与）；
- 不调用任何 API，不访问网络；
- 全部数据在页面内以 mock 形式内置；
- 浏览器直接打开即可运行。

## Mock 链数据

### 纪元规则

```
epoch = height // 100
height 0–99   → 第 0 纪元
height 100    → 触发纪元抬升（旧纪元归档）
height 100–…  → 第 1 纪元
```

### 数据构成

| 数据 | 内容 |
|---|---|
| 创世块 | height=0，显示为剖面最底部花岗岩层「创世内核 · NovScript」，含五条永恒规则 |
| 第 0 纪元 | 高度 1–99 共 99 个区块，由 18 个特性模板循环生成（字符串字面量、列表字面量、if、算术原语、head/tail、strcat、lambda 等），矿工署名取自 5 个地质叙事化名称池 |
| 纪元抬升 | 页面加载约 1.6 秒后自动播放：第 0 纪元下沉变半透明暗褐色，标记「已归档」；第 1 纪元新地表浮现 |
| 第 1 纪元 | 高度 100–102 共 3 个手工定义区块（地层引信原语、列表拼接、回声输出原语） |

### 推演产生的新区块

- 用户点击【推演】后，新岩层编号从 height=103 继续递增；
- 高度自动按 `height // 100` 归入第 1 纪元；
- 提案是否通过由关键词规则决定（含「修改内核 / 严格求值 / 可变绑定 / 删除内核 / 废除 / 推翻 / 破坏内核 / 绕过」→ 拒绝并播放碎裂动画；否则通过）。

### 演示代码的生成

推演成功后，沙箱示例代码按输入关键词简单映射：

| 输入关键词 | 生成的示例代码 |
|---|---|
| 列表 / 方括号 | `(bind xs [1, 2, 3])` + `(head xs)` |
| 字符串 / 拼接 | `(bind s "地层")` + `(echo (strcat s " · 创世"))` |
| if / 条件 | `(if 1 7 9)` |
| 函数 / lambda | `(bind inc (lambda (x) (+ x 1)))` + `(inc 41)` |
| 减法 / 乘法 | `(- 7 2)` / `(* 6 7)` |
| 其他（默认） | `(bind x (+ 1 2))` + `x` |

### 程序画廊

4 个 mock 作品：《地层回声》《化石排序·雏形》《创世祭文》《断层测绘》，点击可载入沙箱运行。

## 与真实项目的对应关系

| 静态原型表现 | 真实 Python 原型 |
|---|---|
| 推演 + 词元计数动画 | PoI：mock_tokenizer 标准词元统计 |
| 岩层凝聚 / 碎裂 | 提案校验通过 / 拒绝 |
| 第 0 纪元归档 | EpochManager 纪元快照 archived 标记 |
| 手电筒钻探 | 查看永久归档的原始地层（只读） |
| 详情面板测试用例 | 区块 Proposal.test_cases |
| 沙箱演示解释器 | novscript/ 完整解释器（惰性求值、thunk） |

## 已知局限（必须如实声明）

1. 本原型展示的是地质叙事与交互流程，不执行任何真实共识、PoI 或语言校验。
2. 内置解释器仅演示，不做惰性求值；真实语义以 Python 原型与白皮书为准。
3. 未来真实系统中的 LLM 摘要存在信息衰减与幻觉风险，摘要链仅线性低速增长。
4. 共识设计仅适合小规模仿真网络。
5. 沙箱为教学级纯计算隔离，不是生产安全容器。
6. 无金融激励；本文件与整个项目均无可交易代币或现实货币价值。
