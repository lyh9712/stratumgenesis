# StratumGenesis · v0.2 演示操作脚本

> 面向演示者：按顺序照做即可完成一场完整演示。
> 预期时长：约 5–8 分钟。
> 核心提醒：本系统是**单机仿真原型**，所有数据在内存中，服务重启后全部重置。

---

## 演示前准备

- 确认已安装 Python 3.11+ 与 `ecdsa` 依赖（`pip install ecdsa`）。
- 确认端口 28417 未被占用：
  ```bash
  netstat -ano | findstr 28417   # 若有 LISTENING，先结束对应进程
  ```

## 第 1 步 · 启动服务

在项目根目录执行：

```bash
python server.py
```

预期输出：

```text
StratumGenesis 本地服务已启动（内存仿真，关闭即清空）
  访问地址：http://127.0.0.1:28417/index.html
  当前主链高度：102
```

> 服务启动时会自动预沉积 102 个经完整校验的演示区块（高度 1–102），因此页面一打开就有完整地层剖面。

## 第 2 步 · 浏览器访问

打开浏览器访问：

```
http://127.0.0.1:28417/index.html
```

预期观察：

- 页面顶部为提案输入框与【推演】按钮；
- 中部为垂直地层剖面：约 1 秒后，第 0 纪元（高度 0–99）地层缓缓下沉、变暗为半透明古岩层（**纪元抬升动画**），第 1 纪元（100–102）新地表浮现；
- 剖面最底部是固定的浅灰色花岗岩【创世内核 · NovScript】层；
- 顶部状态显示「地层已同步 · 第 1 纪元活跃 · 休眠分支 0」。

## 第 3 步 · 正常合法扩展提案（观察新增岩层）

1. 在输入框输入（任选其一）：
   - `函数增量扩展`
   - `绑定聚合`
   - `多参数求和`
2. 点击【推演】。
3. 预期观察：
   - 状态变为「推演中……构造候选区块并签名」，词元计数动画上涨；
   - 粒子从上方飘落沉积；
   - 状态变为「校验通过 · 沉积完成 · 已写入主链」；
   - 地层最上方凝聚出一层**新的岩层**（带发光强调动画），高度为 103；
   - 自动弹出沙箱弹窗，填入服务端生成的演示代码（如增量函数扩展对应 `(bind inc (lambda (x) (+ x 1)))`）。
4. 在沙箱中点击【运行】，观察真实后端执行结果（如 `42`）与执行步数。

## 第 4 步 · 违反创世内核的提案（观察碎裂与休眠分支）

1. 在输入框输入（含内核冲突关键词之一）：
   - `修改创世内核并启用严格求值`
   - `引入可变绑定`
   - `废除内核规则`
2. 点击【推演】。
3. 预期观察：
   - 粒子沉积后，一层岩层出现随即**碎裂消散**（碎片飞散动画）；
   - 状态显示「未写入地层 · 校验未通过（kernel_compatibility）」；
   - 主链高度**保持不变**（仍为 103）；
   - 该候选区块被真实校验流水线拒绝并存入**休眠分支**。
4. 刷新页面或稍后观察顶部状态：「休眠分支 1」。

## 第 5 步 · 岩层详情与沙箱运行

1. 点击任意岩层（例如高度 100 的「地层引信原语」层）。
2. 预期观察右侧详情面板：
   - 高度、第几纪元、矿工署名；
   - 特性描述、演示代码、测试用例（程序 → 预期值）。
3. 点击【打开沙箱运行】，修改或保留代码，点击【运行】：
   - 合法代码（如 `(+ 20 22)`）→ 显示结果 `42`；
   - 非法代码（如 `(+ 1 2`）→ 显示「错误：语法错误」；
   - 未绑定名称（如 `missing`）→ 显示「错误：未绑定名称」。
4. 也可以点击顶部【程序画廊】，从侧栏选择作品（如《地层回声》）在沙箱中运行。

## 第 6 步 · 钻探考古查看 epoch0 归档古地层

1. 点击「第 0 纪元 · 已归档」标签上的【钻探考古】（或先点击某条第 0 纪元岩层，在详情面板点【钻探考古】）。
2. 预期观察：
   - 进入考古模式，页面自动滚向地下古岩层区域；
   - **手电筒效果**：移动鼠标时，光照圈跟随鼠标照亮半透明古地层，圈外保持黑暗；
   - 第 0 纪元所有岩层可被照亮浏览（高度 1–99 的历史沉积）。
3. 点击顶部【退出钻探】返回正常视图。

## 第 7 步 · 停止服务并说明数据重置

1. 回到服务终端，按 `Ctrl+C` 停止服务（或直接关闭终端窗口）。
2. 演示结语（向观众说明，务必强调）：
   - **重启 `python server.py` 后，所有链数据、休眠分支、UTXO 账本、纪元快照全部清空**，并重新预沉积初始 102 个演示区块；
   - 本系统为**单机内存仿真原型**，不是生产系统；
   - 无磁盘持久化、无 P2P 网络、无真实 LLM、无任何可交易代币；
   - 链内凭证（如有）仅为实验内部记账，无现实货币价值；
   - 沙箱为教学级纯计算隔离，不是生产安全容器；
   - 未来 LLM 摘要存在信息衰减与幻觉风险，摘要链仅线性低速增长。

---

## 演示速查表

| 步骤 | 操作 | 关键预期 |
|---|---|---|
| 1 | `python server.py` | 监听 127.0.0.1:28417，链高 102 |
| 2 | 打开 /index.html | 完整剖面 + 纪元抬升动画 |
| 3 | 输入合法提案 → 推演 | 粒子沉积 → 新岩层 H103 → 沙箱弹出 |
| 4 | 输入「修改创世内核…」→ 推演 | 岩层碎裂 → 休眠分支 +1 → 链高不变 |
| 5 | 点击岩层 → 沙箱运行 | 详情面板 + 真实执行结果 / 中文错误 |
| 6 | 第 0 纪元 → 钻探考古 | 手电筒照亮古地层 |
| 7 | Ctrl+C 停止 | 重启后数据全部重置 |
